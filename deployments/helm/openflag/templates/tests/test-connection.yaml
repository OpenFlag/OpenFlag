apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openflag.fullname" . }}-test-connection"
  labels:
{{ include "openflag.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "openflag.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
